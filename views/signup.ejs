<%- include('partials/header'); -%>
    <form  method="POST" class = "sign-up-card" >
        <h1>साइनअप</h1> 
        <div>
            <label for="displayName">नाव</label>
            <input type="text" id="displayName" name="displayName" placeholder="नाव"  value="<%= oldInput.displayName %>"required>
        </div>
        <div>
            <label for="email">ईमेल</label>
            <input class="
            <%= validationErrors.find(e=>e.param === 'username') ? "invalid":"" %>


            <% if(errorMessage){ %>
                <% if(errorMessage == 'User Already Exists'){ %>
                    invalid
                <% } %>
            <% } %>"
            
            id="email" 
            name="username" 
            placeholder="ईमेल" 
            value="<%= oldInput.username %>" 
            >   
        </div>
         
         
         <div class="email error">
            <%= validationErrors.find(e=>e.param === 'username') ? "Please enter valid email.":"" %>
        </div>
        <div class="email error">
            <% if(errorMessage){ %>
                <% if(errorMessage == 'User Already Exists'){ %>
                    <%=errorMessage  %>
                <% } %>
            <% } %>
        </div>
        <div>
            <label for="password">पासवर्ड</label>
            <input class="<%= validationErrors.find(e=>e.param === 'confirmPassword') ? "invalid":"" %>"
            type="password" id="password" name="password" placeholder="पासवर्ड" value="<%= oldInput.password %>" >
        </div>
        <div class="email error">
            <%= validationErrors.find(e=>e.param === 'password') ? "The minimum password length is 5 characters.":"" %>
        </div>
        <div>
            <label for="confirmPassword">पासवर्डची पुष्टी करा</label>
            <input class="<%= validationErrors.find(e=>e.param === 'confirmPassword') ? "invalid":"" %>" 
            type="password" id="confirmPassword" name="confirmPassword" placeholder="पासवर्डची पुष्टी करा" value="<%= oldInput.confirmPassword %>" >
        </div>
        <div class="password error">
            <% if(errorMessage){ %>
                <% if(errorMessage == 'Password Have To Match'){ %>
                    <%=errorMessage  %>
                <% } %>
            <% } %>
        </div>
        <button id= "submit-btn" type="submit " class="btn">
            साइनअप </button>
        
    </form>

    <%- include('partials/footer'); -%>
